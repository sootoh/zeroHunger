@page
@model ZeroHunger.Pages.DryFoodDonationF.IndexModel
@{
}

<br />
<div class="container row p-0 m-0">
    <div class="col-8">
        <h2>Dry Food Donation</h2>
    </div>
    <div clss="col-4">
        <a asp-page="AddDryFoodDonation" class="btn btn-info">Create New Donation</a>
        <button align="center" class="btn btn-danger" onclick="javascript:generatePDF();">Generate PDF</button>
    </div>

    <div class="col-12 border p-3 mt-3 " id="printTable">
        <table id="DT_load" class="table table-striped table-bordered" style="width:100%">        
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Quantity</th>
                    <th>Pick Up Date</th>
                    <th>Remark</th>
                    <th></th>
                </tr>
            </thead>
        </table>
    </div>


    @section Scripts{
        <script src="~/js/DryFoodTable.js"></script>
        <script>
        function generatePDF() {
            var pTable = document.getElementById('printTable').innerHTML;
            var win = window.open('', '', 'height=100%,width=100%');
            win.document.write('<html><head><title>Dry Food Donation History</title>');
            win.document.write('<style> h2{text-align:center;} #printTable{margin-left:auto; margin-right:auto;} th,td{border:1px solid black;} table,th,td{border-collapse:collapse; margin:20px auto;} th:last-child,td:last-child{display:none; border:0;} td{text-align:center;}</style>');
            win.document.write('</head><body>');
            win.document.write('<h2>Dry Food Donation History</h2>');
            win.document.write(pTable);
            win.document.write('</body></html>');
            win.document.close();
            win.print(); 
            setTimeout(function(){win.close()}, 500);  
        }
        </script> 
    }

    
    @*<form method="Post">
            @if (Model.DFD.Count() > 0)
            {
                <table class="table table-striped border">
                    <tr class="table-secondary">
                        <th>
                            <label asp-for="DFD.FirstOrDefault().DryFoodName">Food Name</label>
                        </th>
                        <th>
                            <label asp-for="DFD.FirstOrDefault().DryFoodQuantity">Quantity</label>
                        </th>
                        <th>
                            <label asp-for="DFD.FirstOrDefault().DryFoodPickDate">Pick-up Date</label>
                        </th>
                        <th>
                            <label asp-for="DFD.FirstOrDefault().DryFoodRemark">Remark</label>
                        </th>
                        <th>
                            <label asp-for="DFD.FirstOrDefault().deliverer_Id">Deliverer</label>
                        </th>
                    </tr>
                    @foreach (var item in Model.DFD)
                    {
                <tr>
                    <td>
                        @Html.DisplayFor(m => item.DryFoodName)

                    </td>
                    <td>
                        @Html.DisplayFor(m => item.DryFoodQuantity)

                    </td>
                    <td>
                        @Html.DisplayFor(m => item.DryFoodPickDate)

                    </td>
                    <td>
                        @Html.DisplayFor(m => item.DryFoodRemark)

                    </td>
                    <td>

                        @Html.DisplayFor(m => item.deliverer_Id)

                    </td>

                    <td>

                        <a class="btn btn-success btn-sm text-white" asp-page="EditDryFoodDonation" asp-route-Id="@item.Id">Edit</a>
                    </td>
                </tr>
                    }
                </table>
            }
            else
            {
                <p> No Donation</p>
            }
        </form>*@
    @*</div>*@
</div>
